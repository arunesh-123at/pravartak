
  # Student Dropout Prediction App

  This is a code bundle for Student Dropout Prediction App. The original project is available at https://www.figma.com/design/GW1wS1OOio8uuq1yjK5EmP/Student-Dropout-Prediction-App.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  